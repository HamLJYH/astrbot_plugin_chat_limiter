# 更新日志

本插件的更新遵循约定式提交

所有更新内容都会集中写在这里

---

## v1.0.3 (2026-09-10)

新增

- 支持的平台适配器
- 已在aiocghttp (NapCat) 完成测试
- 其他平台理论上可用，但未验证
- 支持按平台启用限制，未启用的平台直接放行
- 白名单按平台独立管理，同一 ID 可在不同平台设置不同信任状态
- 新增 `/limiterplatforms` 指令，查看当前启用限制的平台列表
- 新增 `enabled_platforms` 配置项，可配置启用限制的平台列表

变更

- `/trust`、`/untrust` 指令改为先输入 ID，再输入平台
- `/trustlist` 支持按平台筛选查看
- `/checktrust` 显示用户生效的所有平台
- 所有指令中的 `QQ号` 统一改为 `ID`

数据结构

- 白名单数据结构从扁平列表改为按平台分组
- 旧版本数据自动迁移到 `default` 全局平台

---

## v1.0.2 (2026-08-28)

重命名插件为访客限制插件

---

## v1.0.1 (2026-08-28)

删除了部分无用指令